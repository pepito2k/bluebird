<div class="page-header">
  <h1>Sources</h1>
</div>

<%= link_to new_admin_source_path, class: "btn btn-success" do %>
    <i class="icon-plus-sign icon-white"></i>
    New Source
<% end %>

<table class="table table-striped">
  <thead>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Actions</th>
  </tr>
  </thead>
  <tbody>
  <% @sources.each do |c| %>
      <tr>
        <td><%= c.id %></td>
        <td><%= c.name %></td>
        <td>
          <%= link_to edit_admin_source_path(c.id), class: "btn btn-mini" do %>
              <i class="icon-pencil"></i>
              Edit
          <% end %>
          <%= link_to admin_source_path(c.id), class: "btn btn-danger btn-mini", confirm: "Are you sure you want to delete this source?", method: :delete do %>
              <i class="icon-trash icon-white"></i>
              Delete
          <% end %>
        </td>
      </tr>
  <% end %>
  </tbody>
  <tfoot>
  <tr>
    <td colspan="3"><%= pluralize @sources.size, "Source" %> found</td>
  </tr>
  </tfoot>
</table>